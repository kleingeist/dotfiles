- clean: ['~']
- link:
    # ~/.dotfiles: ''
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vim/vimrc
    ~/.vim: vim/

- shell: 
  - [git submodule update --init --recursive, Update submodules]
